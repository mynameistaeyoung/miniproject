import styled from "styled-components";

const ListContainer=styled.div`
    padding: 0 24px;
`
const ListWrapper=styled.div`
    display: flex;
    flex-wrap: wrap;
    gap: 12px;
`

export {ListContainer,ListWrapper}